# Archived files

These files are **not deployed** to Netlify and are kept for historical reference only.

The canonical application lives in [`../frontend/`](../frontend/), which Netlify publishes per [`../netlify.toml`](../netlify.toml).

| File | Superseded by |
|------|----------------|
| `index.html` | `frontend/index.html` |
| `oldoneappfor.js` | `frontend/js/app.js` |

The archived Cloud Transcoding UI was a simpler single-input/single-output demo. The deployed frontend supports multiple inputs/outputs, watermarks, builder-token acquire, and template management aligned with the Agora RTSC REST API.
